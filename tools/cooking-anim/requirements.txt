# Install torch (+ optional torchvision) separately first, matched to the
# server's CUDA — see README. These are the rest of the pipeline deps.
diffusers>=0.32.0
transformers>=4.46.0
accelerate>=1.0.0
safetensors>=0.4.5
huggingface_hub>=0.26.0
sentencepiece>=0.2.0
protobuf>=4.25.0
Pillow>=10.4.0
imageio>=2.34.0

# Only needed for the legacy transparent/illustration path (rembg alpha cut-out).
# Not used by the default 3D/CGI banner pipeline — uncomment if you want it.
# rembg>=2.0.59
# onnxruntime-gpu>=1.18.0
